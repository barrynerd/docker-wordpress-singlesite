WORDPRESS_DB_HOST="db"
WORDPRESS_DB_USER="singlesite_user"
WORDPRESS_DB_PASSWORD="db-password"
WORDPRESS_DB_NAME="wordpress_singlesite"
WORDPRESS_TABLE_PREFIX="wp_"
WORDPRESS_DEBUG=0

WP_URL="http://localhost:8000"
WP_TITLE="Example Site"
WP_ADMIN_USER="barry"
WP_ADMIN_PASSWORD="admin-password"
WP_ADMIN_EMAIL=bcaplan@gmail.com

WORDPRESS_PORT=8000
WORDPRESS_DB_PORT=3306
PMA_PORT=8001
DB_PORT=3306

SSL_CERT_FILE=/etc/apache2/ssl/wordpress.crt
SSL_KEY_FILE=/etc/apache2/ssl/wordpress.key

# feature flags

# set to true to install plugins
# set to false to skip plugin installation
# this is a feature flag, so it can be set to true or false
# in the .env file or in the docker-compose.yml file
# the value in the .env file will take precedence

# must be lowercase
INSTALL_PLUGINS=true
INSTALL_PLUGINS_FILE=plugins_to_install.txt
INSTALL_PLUGINS_SHOW_COMMENTS=true
INSTALL_WOOCOMMERCE=true


